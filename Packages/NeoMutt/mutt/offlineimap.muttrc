# Supporting file for Mutt and OfflineIMAP

# Main Options

set mbox_type = Maildir
set folder = "~/.maildir"
set spoolfile = "+gmail/inbox"

# IMAP: offlineimap
source ~/.mutt/mailboxes
# mailboxes `find ~/.maildir/ -type d -name cur -printf '%h '`
