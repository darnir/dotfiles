set from = "darnir@mpi-inf.mpg.de"
set sendmail  = "/usr/bin/msmtp -a MPIINF"

set postponed = "+MPIINF/Drafts"
set spoolfile = "+MPIINF/INBOX"
set record = "+MPIINF/Sent"
unset trash

# color status cyan  default

# TODO: Attempt to create a local Trash can and Archive.
macro index,pager d "<delete-message>" "Delete"
# macro index,pager S "<save-message>+darnirmlist/spam<enter>"  "Mark message as spam"

# folder-hook darnirmlist/trash macro index,pager d "<delete-message><enter>" "Trash"
