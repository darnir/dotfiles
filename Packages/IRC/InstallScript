#!/bin/bash

PACKAGE_NAME="IRC"

install() {
    install_directory irssi home false true
}

post_install() {
    if ! isarchlinux; then
        echo "Please install relevant Perl packages"
    else
        sudo pacman -S perl-lwp-protocol-https perl-text-charwidth perl-net-dbus
    fi
}
